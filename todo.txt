- Check if thiner lines can be drawn
- Check if strokeWidth can handle a value less than 1
- Add a hook for keyPressed in Framework
- Try enable/disable double buffering in swing. Use morphing as an example
- Integrate JavaFX